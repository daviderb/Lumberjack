defaults:
  - _self_
  - dataset: moonsunbalanced
  - model: dp_sparse_random_forest


plotting:
  enabled: true
  show: true

hydra:
  run:
    dir: ./outputs/${dataset.name}-${now:%Y-%m-%d-%H-%M-%S}
